import styled from 'styled-components';

const Title = styled.h1`
    margin: 1rem 0;
    width: 100%;
    font-size: 3rem;
    font-weight: bold;
`

const Paragraph = styled.p`
    margin: 1rem 0;
`

const A = styled.a`
    font-weight: bold;
    color: #000;
`

export { Title, Paragraph, A }